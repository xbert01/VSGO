import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import {
  faCaretUp,
  faCaretDown,
  faEquals,
} from "@fortawesome/free-solid-svg-icons";

  export const arrowUp = <FontAwesomeIcon icon={faCaretUp} />;
  export const arrowDown = <FontAwesomeIcon icon={faCaretDown} />;
  export const equals = <FontAwesomeIcon icon={faEquals} />;